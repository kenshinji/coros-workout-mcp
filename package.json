{
  "name": "coros-workout-mcp",
  "version": "1.0.0",
  "description": "MCP server for creating COROS strength workouts via the Training Hub API",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "coros-workout-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare-catalog": "tsx scripts/prepare-catalog.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/index.js",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck:worker": "tsc --noEmit -p tsconfig.worker.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "@types/node": "^22.15.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1",
    "wrangler": "^4.129.0"
  }
}
